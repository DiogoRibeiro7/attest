% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ledger.R
\name{ledger_find}
\alias{ledger_find}
\title{Find the ledger entry for a model}
\usage{
ledger_find(ledger, x)
}
\arguments{
\item{ledger}{An \code{\link[=attest_ledger]{attest_ledger()}}.}

\item{x}{An \code{attested_model}, or a certificate id.}
}
\value{
The matching entry, or \code{NULL}.
}
\description{
Find the ledger entry for a model
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(1000), x2 = rnorm(1000))
d$y <- factor(rbinom(1000, 1, plogis(d$x1 - d$x2)))
m <- attest_fit(attest_spec(), y ~ x1 + x2, d, engine_glm(), quiet = TRUE)
led <- attest_ledger(tempfile(fileext = ".ndjson"))
ledger_append(led, m)
ledger_find(led, m)$certificate_id
}
\seealso{
Other ledgers: 
\code{\link{attest_ledger}()},
\code{\link{attest_manifest}()},
\code{\link{ledger_append}()},
\code{\link{ledger_entries}()},
\code{\link{ledger_verify}()}
}
\concept{ledgers}
