% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{shift_c2st}
\alias{shift_c2st}
\title{Shift check: classifier two-sample test on the prediction batch}
\usage{
shift_c2st(
  auc_min = 0.6,
  alpha = 0.05,
  max_ref = 2000,
  min_batch = 50,
  interactions = TRUE
)
}
\arguments{
\item{auc_min}{Minimum held-out AUC before a batch is flagged, an effect
size floor.}

\item{alpha}{Significance level for the test.}

\item{max_ref}{Maximum number of training rows stored as the reference
sample. Larger gives more power and a larger certificate.}

\item{min_batch}{Minimum incoming rows before the test is attempted.}

\item{interactions}{Passed to \code{\link[=attest_c2st]{attest_c2st()}}; keep \code{TRUE} to detect changes
in the dependence between features.}
}
\value{
An \code{attest_check}.
}
\description{
Stores a reference sample of the training rows. At prediction time a
classifier is asked to tell that reference from the incoming batch, using
held-out scores; an AUC near 0.5 means the two are indistinguishable. See
\code{\link[=attest_c2st]{attest_c2st()}} for the mechanics.
}
\details{
This complements \code{\link[=shift_monitor]{shift_monitor()}} rather than replacing it. PSI is computed
per feature on binned marginals, so it cannot see a change in the
relationship \emph{between} features. Two samples with identical marginals but
opposite correlation produce no PSI signal at all, while the classifier
separates them easily. Conversely PSI is cheaper, needs no reference sample
stored in the certificate, and says which feature moved.

A batch is flagged only when the test is both significant at \code{alpha} and the
held-out AUC reaches \code{auc_min}. The second bar matters because a large enough
batch makes an arbitrarily small difference significant, and a shift too
small to separate the samples is rarely a shift worth acting on.
}
\examples{
shift_c2st()
# demand a clearer separation before flagging
shift_c2st(auc_min = 0.7)
}
\seealso{
Other shift checks: 
\code{\link{attest_c2st}()},
\code{\link{attest_density_ratio}()},
\code{\link{shift_monitor}()}
}
\concept{shift checks}
