% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ledger.R
\name{ledger_append}
\alias{ledger_append}
\title{Append a certificate to a ledger}
\usage{
ledger_append(ledger, x, note = NULL)
}
\arguments{
\item{ledger}{An \code{\link[=attest_ledger]{attest_ledger()}}.}

\item{x}{An \code{attested_model}.}

\item{note}{Optional free text stored with the entry, such as the run or
pipeline that produced the model.}
}
\value{
The ledger, invisibly.
}
\description{
Append a certificate to a ledger
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(1000), x2 = rnorm(1000))
d$y <- factor(rbinom(1000, 1, plogis(d$x1 - d$x2)))
m <- attest_fit(attest_spec(), y ~ x1 + x2, d, engine_glm(), quiet = TRUE)
led <- attest_ledger(tempfile(fileext = ".ndjson"))
ledger_append(led, m, note = "nightly run")
nrow(ledger_entries(led))
}
\seealso{
Other ledgers: 
\code{\link{attest_ledger}()},
\code{\link{attest_manifest}()},
\code{\link{ledger_entries}()},
\code{\link{ledger_find}()},
\code{\link{ledger_verify}()}
}
\concept{ledgers}
