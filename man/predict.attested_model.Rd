% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.attested_model}
\alias{predict.attested_model}
\title{Predict with validity status}
\usage{
\method{predict}{attested_model}(object, newdata, enforce = TRUE, ...)
}
\arguments{
\item{object}{An \code{attested_model}.}

\item{newdata}{Data to predict on.}

\item{enforce}{If \code{FALSE}, refused rows still receive predictions (status is
kept). Overriding the contract is recorded in the returned object's
\code{"enforced"} attribute.}

\item{...}{Unused.}
}
\value{
A tibble of class \code{attested_prediction}.
}
\description{
Returns an \code{attested_prediction}: a tibble with \code{.pred}, an interval
(\code{.lower}/\code{.upper} for regression, \code{.set} for classification), a
\code{.status} of \code{"valid"}, \code{"flagged"} or \code{"refused"}, a row-level \code{.shift}
score (share of features outside the central training range) and a
\code{.reason}. Refused rows have \code{NA} predictions unless \code{enforce = FALSE}.
}
\details{
When the model was fitted with \code{conformal_split(weighted = TRUE)} the
interval is not fixed. Calibration scores are reweighted by an estimated
covariate density ratio between the calibration set and this batch, so each
row gets its own quantile and intervals widen on drifted input. The estimated
ratio is returned in a \code{.weight} column.

\code{.status} keeps its meaning throughout: \code{"flagged"} still reports that the
batch moved, not that the interval changed. A row whose reweighted quantile
is infinite -- the calibration set holds no comparable evidence -- is
refused, with reason \code{"no conformal evidence after reweighting"}.
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(1000), x2 = rnorm(1000))
d$y <- factor(rbinom(1000, 1, plogis(d$x1 - d$x2)))
m <- attest_fit(attest_spec(), y ~ x1 + x2, d, engine_glm(), quiet = TRUE)
predict(m, d[1:3, ])
# a row outside the certified support is refused
nd <- d[1:2, ]
nd$x1[1] <- 50
predict(m, nd)
}
