% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{attest_fit}
\alias{attest_fit}
\title{Fit a model and issue a certificate}
\usage{
attest_fit(
  spec,
  formula,
  data,
  engine = engine_glm(),
  waive = character(0),
  reason = NULL,
  quiet = FALSE
)
}
\arguments{
\item{spec}{An \code{\link[=attest_spec]{attest_spec()}}.}

\item{formula}{Model formula. The outcome sets the task: numeric gives
regression, a logical or two-level factor gives binary classification,
and a factor with more than two levels gives multiclass.}

\item{data}{A data frame.}

\item{engine}{An engine, see \link{engines}.}

\item{waive}{Character vector of check ids to waive. Every waiver requires
a \code{reason}.}

\item{reason}{Character string explaining the waiver. Stored in the
certificate and printed in every report.}

\item{quiet}{Suppress progress output.}
}
\value{
An object of class \code{attested_model}.
}
\description{
Fit a model and issue a certificate
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(2000), x2 = rnorm(2000))
d$y <- factor(rbinom(2000, 1, plogis(d$x1 - d$x2)))
m <- attest_fit(attest_spec(), y ~ x1 + x2, d, engine_glm(), quiet = TRUE)
certificate(m)
predict(m, d[1:3, ])
}
\seealso{
Other fitting: 
\code{\link{attest_spec}()}
}
\concept{fitting}
