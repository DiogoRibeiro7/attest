% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{new_check}
\alias{new_check}
\title{Define a new check}
\usage{
new_check(id, run, blocking = TRUE, stage = c("pre", "post"), label = NULL)
}
\arguments{
\item{id}{Unique identifier.}

\item{run}{Function of one argument (the context list).}

\item{blocking}{If \code{TRUE} a \code{"fail"} blocks certification.}

\item{stage}{One of \code{"pre"} (before fitting; sees only data) or \code{"post"}
(after fitting; sees the model).}

\item{label}{Human-readable label used in console output, certificates and
reports. Defaults to a title-cased version of \code{id}.}
}
\value{
An object of class \code{attest_check}.
}
\description{
Checks are the extension point of attest. A check is a function
\code{run(ctx)} receiving a context list with \code{train}, \code{test}, \code{calib},
\code{model}, \code{task}, \code{outcome}, \code{features}, \code{spec} and returning an
\code{\link[=attest_result]{attest_result()}}.
}
\examples{
# a check that refuses training data with too many missing cells
max_missing <- new_check("max_missing", stage = "pre", run = function(ctx) {
  prop <- mean(is.na(ctx$train[ctx$features]))
  attest_result("max_missing", if (prop <= 0.05) "pass" else "fail",
    statistic = prop, threshold = 0.05
  )
})
max_missing
}
\seealso{
Other checks: 
\code{\link{attest_boot}()},
\code{\link{attest_result}()},
\code{\link{attest_verdict}()},
\code{\link{imbalance_report}()}
}
\concept{checks}
