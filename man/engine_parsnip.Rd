% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engines.R
\name{engine_parsnip}
\alias{engine_parsnip}
\title{Fit any parsnip model under a certificate}
\usage{
engine_parsnip(spec)
}
\arguments{
\item{spec}{A parsnip \code{model_spec}, such as \code{parsnip::logistic_reg()}.}
}
\value{
An object of class \code{attest_engine}.
}
\description{
Wraps a parsnip model specification, so anything parsnip can fit --
\code{boost_tree()} on xgboost, \code{linear_reg()} on glmnet, \code{rand_forest()} on
ranger, and the rest -- can be certified without writing an adapter for each.
}
\details{
The specification's mode is set from the task inferred by \code{\link[=attest_fit]{attest_fit()}}, so
the usual \code{set_mode()} call is unnecessary. A mode that contradicts the
outcome is an error rather than a silent correction.

The certificate records the specification rather than the bare word
\code{"parsnip"}, as \verb{parsnip:<model>/<engine>} -- for example
\code{parsnip:boost_tree/xgboost} -- so a model card says what was actually
fitted.

attest needs a probability for the second outcome level, which parsnip
supplies through \code{type = "prob"}. An engine offering no probabilities cannot
be calibrated or given conformal label sets, and is rejected at fit time
rather than issuing a certificate whose checks mean nothing.
}
\examples{
if (requireNamespace("parsnip", quietly = TRUE)) {
  engine_parsnip(parsnip::logistic_reg())
}
}
