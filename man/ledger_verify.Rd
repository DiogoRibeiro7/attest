% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ledger.R
\name{ledger_verify}
\alias{ledger_verify}
\title{Verify the integrity of a ledger chain}
\usage{
ledger_verify(ledger)
}
\arguments{
\item{ledger}{An \code{\link[=attest_ledger]{attest_ledger()}}.}
}
\value{
A logical, \code{TRUE} when the chain is intact, with attribute
\code{"problems"} describing any breaks.
}
\description{
Recomputes each entry's digest and checks that it carries the digest of the
entry before it. An entry that has been altered fails on its own digest;
an entry that has been removed or reordered breaks the link in the entry
that followed it.
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(1000), x2 = rnorm(1000))
d$y <- factor(rbinom(1000, 1, plogis(d$x1 - d$x2)))
m <- attest_fit(attest_spec(), y ~ x1 + x2, d, engine_glm(), quiet = TRUE)
led <- attest_ledger(tempfile(fileext = ".ndjson"))
ledger_append(led, m)
ledger_verify(led)
}
\seealso{
Other ledgers: 
\code{\link{attest_ledger}()},
\code{\link{attest_manifest}()},
\code{\link{ledger_append}()},
\code{\link{ledger_entries}()},
\code{\link{ledger_find}()}
}
\concept{ledgers}
