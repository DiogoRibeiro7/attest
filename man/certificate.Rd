% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{certificate}
\alias{certificate}
\alias{is_sealed}
\alias{verify}
\alias{unseal}
\title{Inspect and verify certificates}
\usage{
certificate(x)

is_sealed(x)

verify(x, data = NULL, ledger = NULL)

unseal(x)
}
\arguments{
\item{x}{An \code{attested_model}.}

\item{data}{Training data used to fit the model, for hash verification.}

\item{ledger}{An optional \code{\link[=attest_ledger]{attest_ledger()}}. When given, \code{verify()} also
checks that this certificate was recorded, that its hashes match the
record, and that the ledger chain is intact.}
}
\value{
\code{certificate()} returns the certificate; \code{is_sealed()} a logical;
\code{verify()} a logical with attribute \code{"diff"} listing what changed;
\code{unseal()} the raw engine object.
}
\description{
Inspect and verify certificates
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(1000), x2 = rnorm(1000))
d$y <- factor(rbinom(1000, 1, plogis(d$x1 - d$x2)))
m <- attest_fit(attest_spec(), y ~ x1 + x2, d, engine_glm(), quiet = TRUE)
certificate(m)
is_sealed(m)
verify(m)
class(unseal(m))
}
\seealso{
Other certificates: 
\code{\link{attest_manifest}()}
}
\concept{certificates}
