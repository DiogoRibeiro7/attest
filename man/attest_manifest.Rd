% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ledger.R
\name{attest_manifest}
\alias{attest_manifest}
\title{Build a portable manifest for an attested model}
\usage{
attest_manifest(x)
}
\arguments{
\item{x}{An \code{attested_model}.}
}
\value{
A named list.
}
\description{
A manifest is the certificate reduced to the facts that identify it: the
three hashes, what was fitted, and the environment it was fitted in. It is
plain data, so it can be written to a file, sent to someone who does not
have the model, and compared later.
}
\examples{
set.seed(1)
d <- data.frame(x1 = rnorm(1000), x2 = rnorm(1000))
d$y <- factor(rbinom(1000, 1, plogis(d$x1 - d$x2)))
m <- attest_fit(attest_spec(), y ~ x1 + x2, d, engine_glm(), quiet = TRUE)
str(attest_manifest(m))
}
\seealso{
Other certificates: 
\code{\link{certificate}()}

Other ledgers: 
\code{\link{attest_ledger}()},
\code{\link{ledger_append}()},
\code{\link{ledger_entries}()},
\code{\link{ledger_find}()},
\code{\link{ledger_verify}()}
}
\concept{certificates}
\concept{ledgers}
