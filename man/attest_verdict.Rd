% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{attest_verdict}
\alias{attest_verdict}
\title{Decide a check verdict from an interval and a threshold}
\usage{
attest_verdict(statistic, ci, threshold, direction = c("at_most", "at_least"))
}
\arguments{
\item{statistic}{The point estimate.}

\item{ci}{Confidence interval for \code{statistic}, length 2.}

\item{threshold}{The threshold to compare against.}

\item{direction}{\code{"at_most"} when the statistic should not exceed the
threshold (ECE, PSI), \code{"at_least"} when it should not fall below it
(coverage).}
}
\value{
One of \code{"pass"}, \code{"weak"}, \code{"fail"}.
}
\description{
Implements the rule that a check fails only when the whole confidence
interval clears the threshold, so that noise alone cannot fail a check.
When the interval straddles the threshold the verdict is \code{"weak"}: the
evidence does not settle the question either way.
}
\details{
If \code{ci} is \code{c(NA, NA)} the point estimate decides, and \code{"weak"} is never
returned.
}
\examples{
attest_verdict(0.04, c(0.02, 0.09), threshold = 0.05, direction = "at_most")
attest_verdict(0.04, c(0.02, 0.045), threshold = 0.05, direction = "at_most")
}
